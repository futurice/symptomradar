import React from 'react';
import styled from 'styled-components';

const MapContainer = styled.div``;

function Map() {
  return (
    <MapContainer>
      <p>[map]</p>
    </MapContainer>
  );
}

export default Map;
