import React from 'react';

/* Placeholder. Edit, rename or replace as necessary. */

type ModalContentProps = {
  region: string;
};

const ModalContent = ({ region }: ModalContentProps) => {
  return <>{region}</>;
};

export default ModalContent;
